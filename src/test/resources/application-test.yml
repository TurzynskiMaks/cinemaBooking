spring:
  datasource:
    url: jdbc:h2:mem:cinema_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true

  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always
mail:
  api-key: test
  base-url: http://localhost